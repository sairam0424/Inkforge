{
  "name": "inkforge",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered article generation system — notes/topic/code → human-readable MDX",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.9.12",
    "typescript": "^5.8.0"
  }
}
